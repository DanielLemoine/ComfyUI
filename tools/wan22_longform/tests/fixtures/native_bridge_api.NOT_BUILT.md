# Native Wan bridge fixture — NOT_BUILT

The fixture is intentionally absent because no official template containing `WanFirstLastFrameToVideo` was verified. `D:\AI\ComfyUI\blueprints\First-Last-Frame to Video.json` contains LTX topology and cannot attest a Wan bridge graph.

Actionable blocker: capture a verified official native Wan FLF template before adding this fixture. Do not substitute community topology or invent node wiring.

The future canonical fixture input titles are `BRIDGE_FIRST_IMAGE` and `BRIDGE_LAST_IMAGE`. `BRIDGE_START` and `BRIDGE_END` are documentation aliases only.
